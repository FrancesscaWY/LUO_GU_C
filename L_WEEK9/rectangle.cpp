#include <iostream>
#include <vector>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    vector<vector<vector<int>>> t_box(a,vector<vector<int>>(b,vector<int>(c,1)));//定义一个三维数组,并初始化为1
    int n;
    cin >> n;
}